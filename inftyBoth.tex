\documentclass[tikz]{standalone}
\usepackage{pgfplots}
\usepackage{python}
\pgfplotsset{compat=1.13}

\begin{document}

\begin{tikzpicture}
\pgfplotsset{every outer x axis line/.style={->}}
\begin{axis}
    [
        name = ax1,
        x post scale=-1,
        axis x line = bottom,
        axis y line = right,
        hide y axis,
        mark size = 1pt,
        xmin = 0, xmax = 0.01,
        ymin = 0.03, ymax = 0.05,
    ]
    \addplot+
    [
        only marks,
        mark = square,
        error bars/.cd, y dir = both, y explicit,
    ] table
    [
        x expr = 1/\thisrowno{1}^2,
        y expr = \thisrowno{2}/\thisrowno{1}^2,
        y error expr = \thisrowno{3}/\thisrowno{1}^2,
  ] {./data/inftyTorus.dat};
\end{axis}
\begin{axis}
    [ 
        name = ax2,
        axis x line = bottom,
        axis y line = left,
        ytick = {0.035, 0.04, 0.045, 0.05},
        every axis y label/.style={
            at={(ticklabel* cs:1.08)},
        anchor=south,},
        ylabel = $\displaystyle \frac{\left<Q^2\right>}{N^2}$,
        at={(ax1.south east)},
        anchor=south west,
        mark size = 1pt,
        xmin = 0, xmax = 0.01,
        ymin = 0.03, ymax = 0.05,
    ]
        \addplot+
        [
            only marks,
            mark = square,
            error bars/.cd, y dir = both, y explicit,
        ] table
        [
            x expr = 1/\thisrowno{1}^2,
            y expr = \thisrowno{2}/\thisrowno{1}^2,
            y error expr = \thisrowno{3}/\thisrowno{1}^2,
        ] {./data/inftyMoeb.dat};
\end{axis}

\node [above=45pt] at (ax1.north east) {\large Infinite volume limit};
\node at (ax1.north) {Torus};
\node at (ax2.north) {M\"obius};
\node [below=20pt] at (ax1.south east) {$\displaystyle \frac{1}{N^2}$};
\end{tikzpicture}
\end{document}

